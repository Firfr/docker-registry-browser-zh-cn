# 本地调试命令记录

本项目的汉化不是原码汉化，是从原项目的Docker镜像中，把前端代码复制出来汉化，在把汉化后的前端代码打包到镜像中。

## 获取前端代码

- 删除旧的前端代码
  ```bash
  rm -rf ./views/* && chmod -R 777 ./views
  ```
- 拉取镜像
  ```bash
  docker pull klausmeyer/docker-registry-browser:1.8.5
  ```
- 运行命令
  ```bash
  docker run --rm \
    --name drb \
    --restart=no \
    -e SECRET_KEY_BASE=docker-registry-browser \
    -v ./views:/views \
    klausmeyer/docker-registry-browser:1.8.5 \
    sh -c "cp -rf /app/app/views/* /views/ && echo '✅ 文件已复制并覆盖，容器即将退出'"
  ```

## 调试运行

- 先部署一个registry用于测试

- 容器运行
  ```bash
  docker run --rm -it --restart=no \
    --name drb --network host \
    -e SECRET_KEY_BASE=docker-registry-browser \
    -e DOCKER_REGISTRY_URL=http://127.0.0.1:5000 \
    -e ENABLE_DELETE_IMAGES=true \
    -e ENABLE_COLLAPSE_NAMESPACES=true \
    -v ./views:/app/app/views \
    klausmeyer/docker-registry-browser:1.8.5
  ```

## 构建镜像测试

```bash
docker run --rm -it --restart=no \
  --name drb --network host \
  -e SECRET_KEY_BASE=docker-registry-browser \
  -e DOCKER_REGISTRY_URL=http://127.0.0.1:5000 \
  -e ENABLE_DELETE_IMAGES=true \
  -e ENABLE_COLLAPSE_NAMESPACES=true \
  firfe/drb_zh-cn:1.8.5
```

